import { NgModule } from '@angular/core';
import { ObservablesShellRoutingModule } from './observables-shell-routing.module';

@NgModule({
  imports: [ObservablesShellRoutingModule],
})
export class ObservablesShellModule {}
